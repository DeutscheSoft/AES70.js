import { Enum8 } from './Enum8.js';

/**
 * Interpolation law for ramper to use.
 * @category Types
 * @class OcaObserverState
 * @extends Enum8
 */
export const OcaObserverState = Enum8({
  NotTriggered: 0,
  Triggered: 1,
});
