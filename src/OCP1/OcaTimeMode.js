import { Enum8 } from './Enum8.js';

/**
 * Time mode of <b>OcaTask </b>agent.
 * @category Types
 * @class OcaTimeMode
 * @extends Enum8
 */
export const OcaTimeMode = Enum8({
  Absolute: 1,
  Relative: 2,
});
