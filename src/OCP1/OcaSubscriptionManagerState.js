import { Enum8 } from './Enum8.js';

/**
 * Enum describing <b>OcaSubscriptionManager</b> states.
 * @category Types
 * @class OcaSubscriptionManagerState
 * @extends Enum8
 */
export const OcaSubscriptionManagerState = Enum8({
  Normal: 1,
  EventsDisabled: 2,
});
